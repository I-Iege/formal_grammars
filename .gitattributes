# Auto detect text files, convert to LF when checkin to object database and when checkout to working directory
* text=auto eol=lf

# conanfile.py must use LF also on Windows (see https://github.com/conan-io/conan/issues/7230)
conanfile.py text eol=lf

# These are explicitly windows files and should use crlf
*.bat text eol=crlf
*.cmd text eol=crlf
*.def text eol=crlf
